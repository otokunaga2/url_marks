<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="span8">
    <% if @user.bookmarks.any? %>
      <h3>Microposts (<%= @user.bookmarks.count %>)</h3>
      <ol class="microposts">
        <%= render @bookmarks %>
      </ol>
      <%= will_paginate @bookmarks %>
    <% end %>
  </div>
</div>
